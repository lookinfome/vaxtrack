info:
  name: get users in role
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/api/vaxtrack/v1/userrolemapping/getusersinrole/hospital-admin?contextId=CityCare-17-c4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
