info:
  name: get pending requests
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseURL}}/api/vaxtrack/v1/userrolemapping/getpendingrequests"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
