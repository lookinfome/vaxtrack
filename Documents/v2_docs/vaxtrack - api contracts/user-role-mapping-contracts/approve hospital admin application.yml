info:
  name: approve hospital admin application
  type: http
  seq: 7

http:
  method: PUT
  url: "{{baseURL}}/api/vaxtrack/v1/userrolemapping/approvehospitaladminapplication/1"
  body:
    type: json
    data: |-
      {
        "comment": "Approved after verification."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
