info:
  name: revoke role
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseURL}}/api/vaxtrack/v1/userrolemapping/revokerole/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
