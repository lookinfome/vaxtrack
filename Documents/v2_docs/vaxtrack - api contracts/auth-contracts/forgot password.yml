info:
  name: forgot password
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/api/vaxtrack/v1/auth/forgotpassword"
  body:
    type: json
    data: |-
      {
        "email": "meera@test.com"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
