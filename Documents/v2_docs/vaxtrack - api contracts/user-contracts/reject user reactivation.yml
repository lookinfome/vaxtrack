info:
  name: reject user reactivation
  type: http
  seq: 14

http:
  method: PUT
  url: "{{baseURL}}/api/vaxtrack/v1/user/rejectuserreactivation/DemoUser-17-f7"
  body:
    type: json
    data: |-
      {
        "comment": "Issue still unresolved."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
