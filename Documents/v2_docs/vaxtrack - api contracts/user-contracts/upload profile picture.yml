info:
  name: upload profile picture
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseURL}}/api/vaxtrack/v1/user/uploadprofilepicture"
  body:
    type: multipartForm
    data:
      - name: userId
        value: "DemoUser-17-f7"
        type: text
      - name: file
        value: ""
        type: file
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
