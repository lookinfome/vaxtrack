info:
  name: change password
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseURL}}/api/vaxtrack/v1/user/changepassword"
  body:
    type: json
    data: |-
      {
        "userId": "Vivek-17-a1",
        "currentPassword": "Vaxtrack@Test1",
        "newPassword": "Vaxtrack@NewPass1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
