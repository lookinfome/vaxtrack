info:
  name: update email
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseURL}}/api/vaxtrack/v1/user/updateemail"
  body:
    type: json
    data: |-
      {
        "userId": "DemoUser-17-f7",
        "newEmail": "demo.user.new@vaxtrack.system",
        "currentPassword": "Demo@12345"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
