info:
  name: request reactivation
  type: http
  seq: 8

http:
  method: PUT
  url: "{{baseURL}}/api/vaxtrack/v1/hospital/requestreactivation/CityCare-17-c4"
  body:
    type: json
    data: |-
      {
        "comment": "Issue resolved, requesting reactivation."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
