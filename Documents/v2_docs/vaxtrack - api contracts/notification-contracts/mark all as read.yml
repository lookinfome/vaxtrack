info:
  name: mark all as read
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseURL}}/api/vaxtrack/v1/notification/markallasread"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
