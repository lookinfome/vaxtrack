info:
  name: mark as read
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseURL}}/api/vaxtrack/v1/notification/markasread/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
