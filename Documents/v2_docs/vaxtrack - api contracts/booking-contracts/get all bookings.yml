info:
  name: get all bookings
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseURL}}/api/vaxtrack/v1/booking/getallbookings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
