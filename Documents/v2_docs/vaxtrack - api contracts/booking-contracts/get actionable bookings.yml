info:
  name: get actionable bookings
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseURL}}/api/vaxtrack/v1/booking/getactionablebookings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
