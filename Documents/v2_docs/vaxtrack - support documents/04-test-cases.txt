================================================================================
VAXTRACK — TEST CASES DOCUMENT
Version: 2.0.0   |   Date: 2026-06-26   |   Status: Active
================================================================================

LEGEND
------
TC-ID   : Unique test case identifier
Input   : What is sent in the request
Expected: What the system should return
Layer   : Unit (service) | Integration (controller + DB) | E2E (full stack)

================================================================================
MODULE 1: AUTH
================================================================================

--------------------------------------------------------------------------------
TC-AUTH-001: Successful login with valid credentials
--------------------------------------------------------------------------------
Endpoint : POST /Auth/LoginAsync
Input    : { Email: "user@email.com", Password: "correctPassword" }
Precond  : User registered; credentials exist; account not soft-deleted
Expected : 200 OK
           Response contains: Token (non-empty), ExpiresAt, UserId, UserName,
           Email, UserRole (false for regular user)
           JWT parses to: sub=UserUid, role="user", jti present

TC-AUTH-002: Login fails with wrong password
Endpoint : POST /Auth/LoginAsync
Input    : { Email: "user@email.com", Password: "wrongPassword" }
Expected : 500 (generic error, no specific hint about password vs email)

TC-AUTH-003: Login fails with unregistered email
Endpoint : POST /Auth/LoginAsync
Input    : { Email: "notregistered@email.com", Password: "anyPassword" }
Expected : 500 (same generic message as wrong password — enumeration prevention)

TC-AUTH-004: Login fails for soft-deleted account
Endpoint : POST /Auth/LoginAsync
Input    : { Email: "deleted@email.com", Password: "correctPassword" }
Precond  : User's UserCredentials IsDeleted = true
Expected : 500 (same generic message — no indication of deletion)

TC-AUTH-005: Login returns admin role in token
Endpoint : POST /Auth/LoginAsync
Input    : { Email: "admin@email.com", Password: "adminPassword" }
Precond  : User exists with UserRole = true
Expected : 200 OK; JWT contains claim role = "admin"

TC-AUTH-006: Successful logout
Endpoint : POST /Auth/LogoutAsync
Input    : Valid JWT in Authorization header
Expected : 200 OK; { "message": "logged out successfully" }
           RevokedTokens table now contains jti from the token

TC-AUTH-007: Subsequent request after logout returns 401
Endpoint : GET /User/GetUserProfileDataAsync/{userId}
Input    : Same JWT used in TC-AUTH-006
Expected : 401 Unauthorized (OnTokenValidated rejects the revoked jti)

TC-AUTH-008: Logout without token returns 401
Endpoint : POST /Auth/LogoutAsync
Input    : No Authorization header
Expected : 401 Unauthorized (middleware rejects before controller)

TC-AUTH-009: ForgotPassword with registered email
Endpoint : POST /Auth/ForgotPasswordAsync
Input    : { Email: "user@email.com" }
Precond  : Email is registered to an active account
Expected : 200 OK; Message = generic string; ResetToken non-empty; ExpiresAt set
           PasswordResetTokens table has a new row: IsUsed=false, expiry=+15min

TC-AUTH-010: ForgotPassword with unregistered email (no enumeration)
Endpoint : POST /Auth/ForgotPasswordAsync
Input    : { Email: "unknown@email.com" }
Expected : 200 OK; Message = same generic string; ResetToken = null; ExpiresAt = null
           (Same HTTP status and message as registered email — cannot distinguish)

TC-AUTH-011: ForgotPassword for soft-deleted account
Endpoint : POST /Auth/ForgotPasswordAsync
Input    : { Email: "deleted@email.com" }
Precond  : Account soft-deleted; credentials still exist
Expected : 200 OK; ResetToken = null (generic response, no token issued)

TC-AUTH-012: ResetForgottenPassword with valid token
Endpoint : POST /Auth/ResetForgottenPasswordAsync
Input    : { ResetToken: "<valid token from TC-AUTH-009>", NewPassword: "newPass123" }
Expected : 200 OK; { UserId, UpdatedAt }
           UserCredentials.PasswordHash updated to BCrypt hash of "newPass123"
           PasswordResetTokens.IsUsed = true, UsedAt = UtcNow

TC-AUTH-013: ResetForgottenPassword with expired token
Endpoint : POST /Auth/ResetForgottenPasswordAsync
Input    : { ResetToken: "<token created > 15 min ago>", NewPassword: "newPass" }
Expected : 500 (GetValidTokenAsync returns null for expired tokens)

TC-AUTH-014: ResetForgottenPassword with already-used token
Endpoint : POST /Auth/ResetForgottenPasswordAsync
Input    : { ResetToken: "<token from TC-AUTH-012>", NewPassword: "anotherPass" }
Expected : 500 (IsUsed=true is filtered out by GetValidTokenAsync)

TC-AUTH-015: ResetForgottenPassword with invalid/fabricated token
Endpoint : POST /Auth/ResetForgottenPasswordAsync
Input    : { ResetToken: "thisisnotavalidtoken", NewPassword: "newPass" }
Expected : 500 (no matching row in PasswordResetTokens)

================================================================================
MODULE 2: USER
================================================================================

TC-USER-001: Successful user registration
Endpoint : POST /User/CreateUserAsync
Input    : { FirstName, LastName, Birthdate (past), Gender, Phone, Address,
             PinCode, Email (unique), Password }
Expected : 201 Created
           Location header: /User/GetUserProfileDataAsync/{userId}
           Response: UserId (generated), UserName, Email, UserRole=false, CreatedAt
           Users table: new row with IsDeleted=false, UserRole=false
           UserCredentials table: BCrypt hash stored, plain text absent

TC-USER-002: Registration fails with duplicate email
Endpoint : POST /User/CreateUserAsync
Input    : Email already exists in active UserCredentials
Expected : 500 "email already in use"

TC-USER-003: Registration fails with future birthdate
Endpoint : POST /User/CreateUserAsync
Input    : UserBirthdate = tomorrow's date
Expected : 500 "birth date cannot be today or in the future"

TC-USER-004: Registration fails with today's birthdate
Endpoint : POST /User/CreateUserAsync
Input    : UserBirthdate = today (>= UtcNow)
Expected : 500 (same as future birthdate constraint)

TC-USER-005: User updates own profile
Endpoint : PUT /User/UpdateUserAsync
Input    : JWT (user); { UserId=own, FirstName, LastName, ... }
Expected : 200 OK; updated fields returned; UserModel.UpdatedAt stamped

TC-USER-006: User cannot update another user's profile
Endpoint : PUT /User/UpdateUserAsync
Input    : JWT (user A); { UserId = user B's UserId }
Expected : 403 Forbidden (UnauthorizedAccessException → Forbid())

TC-USER-007: Admin can update any user's profile
Endpoint : PUT /User/UpdateUserAsync
Input    : JWT (admin); { UserId = any user's UserId }
Expected : 200 OK

TC-USER-008: Update profile for non-existent user
Endpoint : PUT /User/UpdateUserAsync
Input    : { UserId: "NONEXISTENT" }
Expected : 500 "user not found"

TC-USER-009: User updates own email with correct current password
Endpoint : PUT /User/UpdateEmailAsync
Input    : JWT (user); { UserId=own, CurrentPassword=correct, NewEmail=new }
Expected : 200 OK; UserCredentials.Email updated

TC-USER-010: User update email fails with wrong current password
Endpoint : PUT /User/UpdateEmailAsync
Input    : JWT (user); { UserId=own, CurrentPassword=wrong, NewEmail=new }
Expected : 500 "current password is incorrect"

TC-USER-011: Admin can update email without knowing current password
Endpoint : PUT /User/UpdateEmailAsync
Input    : JWT (admin); { UserId=anyUser, CurrentPassword="" (ignored), NewEmail=new }
Expected : 200 OK (CurrentPassword field not verified for admin)

TC-USER-012: Update email fails if new email already in use
Endpoint : PUT /User/UpdateEmailAsync
Input    : { NewEmail: "alreadyused@email.com" }
Expected : 500 "email already in use"

TC-USER-013: User cannot update another user's email
Endpoint : PUT /User/UpdateEmailAsync
Input    : JWT (user A); { UserId = user B's UserId }
Expected : 403 Forbidden

TC-USER-014: User changes own password with correct current password
Endpoint : PUT /User/ChangePasswordAsync
Input    : JWT (user); { UserId=own, CurrentPassword=correct, NewPassword=new }
Expected : 200 OK; UserCredentials.PasswordHash updated; UserModel.UpdatedAt stamped

TC-USER-015: Password change fails with wrong current password
Endpoint : PUT /User/ChangePasswordAsync
Input    : JWT (user); { CurrentPassword: "wrong" }
Expected : 500 "current password is incorrect"

TC-USER-016: Admin resets any user's password without current password
Endpoint : PUT /User/ChangePasswordAsync
Input    : JWT (admin); { UserId=anyUser, CurrentPassword="" (ignored), NewPassword=new }
Expected : 200 OK

TC-USER-017: User views own profile
Endpoint : GET /User/GetUserProfileDataAsync/{userId}
Input    : JWT (user); userId = own UserId
Expected : 200 OK; full profile including Email (from UserCredentials)

TC-USER-018: User cannot view another user's profile
Endpoint : GET /User/GetUserProfileDataAsync/{userId}
Input    : JWT (user A); userId = user B's UserId
Expected : 403 Forbidden

TC-USER-019: Admin views any user's profile
Endpoint : GET /User/GetUserProfileDataAsync/{userId}
Input    : JWT (admin); any userId
Expected : 200 OK

TC-USER-020: Get profile for non-existent user
Endpoint : GET /User/GetUserProfileDataAsync/{userId}
Input    : userId = "DOESNOTEXIST"
Expected : 500 "user not found"

TC-USER-021: Admin retrieves all users
Endpoint : GET /User/GetAllUsersAsync
Input    : JWT (admin)
Expected : 200 OK; list of all non-deleted users with emails

TC-USER-022: Non-admin cannot retrieve all users
Endpoint : GET /User/GetAllUsersAsync
Input    : JWT (user)
Expected : 403 Forbidden ([Authorize(Roles="admin")] on endpoint)

TC-USER-023: User deletes own account
Endpoint : DELETE /User/DeleteMyAccountAsync
Input    : JWT (user)
Expected : 204 No Content
           Users.IsDeleted = true
           All bookings soft-deleted, pending slots restored to hospitals
           All role mappings set IsActive = false
           UserCredentials.IsDeleted = true
           User can no longer log in

TC-USER-024: Admin deletes user by userId
Endpoint : DELETE /User/DeleteUserAsync/{userId}
Input    : JWT (admin); userId = target user's UserId
Expected : 204 No Content; same cascade as TC-USER-023

TC-USER-025: Delete non-existent user
Endpoint : DELETE /User/DeleteUserAsync/{userId}
Input    : JWT (admin); userId = "DOESNOTEXIST"
Expected : 500 "user not found"

================================================================================
MODULE 3: HOSPITAL
================================================================================

TC-HOSP-001: Admin creates a hospital
Endpoint : POST /Hospital/CreateHospitalAsync
Input    : JWT (admin); { HospitalName: "City General Hospital" }
Expected : 201 Created
           Response: HospitalId (slug from name), TotalSlots=50, SlotsAvailable=50
           Contact fields empty; RegisteredDate set

TC-HOSP-002: Non-admin cannot create hospital
Endpoint : POST /Hospital/CreateHospitalAsync
Input    : JWT (user)
Expected : 403 Forbidden

TC-HOSP-003: Admin updates hospital contact info
Endpoint : PUT /Hospital/UpdateHospitalAsync
Input    : JWT (admin); { HospitalId, Address, PinCode, Phone, Email }
Expected : 200 OK; contact fields updated; HospitalName/TotalSlots unchanged

TC-HOSP-004: hospital-admin updates own hospital
Endpoint : PUT /Hospital/UpdateHospitalAsync
Input    : JWT (hospital-admin); { HospitalId = assigned hospital }
Precond  : UserRoleMappings row: UserUid=caller, RoleTag="hospital-admin",
           ContextId=HospitalId, IsActive=true
Expected : 200 OK

TC-HOSP-005: hospital-admin cannot update a different hospital
Endpoint : PUT /Hospital/UpdateHospitalAsync
Input    : JWT (hospital-admin); { HospitalId = different hospital }
Expected : 403 Forbidden (IsUserInRoleAsync returns false for different hospitalId)

TC-HOSP-006: Normal user cannot update hospital
Endpoint : PUT /Hospital/UpdateHospitalAsync
Input    : JWT (user); any { HospitalId }
Expected : 403 Forbidden (not admin, no hospital-admin role)

TC-HOSP-007: Update hospital that does not exist
Endpoint : PUT /Hospital/UpdateHospitalAsync
Input    : JWT (admin); { HospitalId: "DOESNOTEXIST" }
Expected : 500 "hospital not found"

TC-HOSP-008: Admin updates total slots
Endpoint : PUT /Hospital/UpdateTotalSlotsAsync/{hospitalId}/{totalSlots}
Input    : JWT (admin); totalSlots = 100
Expected : 200 OK; TotalSlots = 100; SlotsAvailable unchanged (if <= 100)

TC-HOSP-009: Update total slots below current available (clamp)
Endpoint : PUT /Hospital/UpdateTotalSlotsAsync/{hospitalId}/{totalSlots}
Input    : JWT (admin); SlotsAvailable currently = 80; totalSlots = 40
Expected : 200 OK; TotalSlots = 40; SlotsAvailable clamped to 40

TC-HOSP-010: Update total slots to negative value
Endpoint : PUT /Hospital/UpdateTotalSlotsAsync/{hospitalId}/{totalSlots}
Input    : JWT (admin); totalSlots = -1
Expected : 500 "total slots cannot be negative"

TC-HOSP-011: hospital-admin updates own hospital's total slots
Endpoint : PUT /Hospital/UpdateTotalSlotsAsync/{hospitalId}/{totalSlots}
Input    : JWT (hospital-admin); hospitalId = assigned hospital
Expected : 200 OK

TC-HOSP-012: Admin updates available slots (manual delta)
Endpoint : PUT /Hospital/UpdateAvailableSlotsAsync/{hospitalId}/{slotsToUpdate}
Input    : JWT (admin); slotsToUpdate = -5
Expected : 200 OK; SlotsAvailable decremented by 5

TC-HOSP-013: Available slots cannot go below 0
Endpoint : PUT /Hospital/UpdateAvailableSlotsAsync/{hospitalId}/{slotsToUpdate}
Input    : JWT (admin); SlotsAvailable currently = 2; slotsToUpdate = -5
Expected : 500 "would have negative available slots"

TC-HOSP-014: Available slots cannot exceed TotalSlots
Endpoint : PUT /Hospital/UpdateAvailableSlotsAsync/{hospitalId}/{slotsToUpdate}
Input    : JWT (admin); SlotsAvailable=50, TotalSlots=50; slotsToUpdate = +5
Expected : 500 "cannot exceed total slots"

TC-HOSP-015: Any authenticated user can view hospital by ID
Endpoint : GET /Hospital/GetHospitalByIdAsync/{hospitalId}
Input    : JWT (user); valid hospitalId
Expected : 200 OK; full hospital profile

TC-HOSP-016: Get hospital by non-existent ID
Endpoint : GET /Hospital/GetHospitalByIdAsync/{hospitalId}
Input    : hospitalId = "DOESNOTEXIST"
Expected : 500 "hospital not found"

TC-HOSP-017: Any authenticated user can view all hospitals
Endpoint : GET /Hospital/GetAllHospitalsAsync
Input    : JWT (user)
Expected : 200 OK; list of non-deleted hospitals

TC-HOSP-018: Admin deletes hospital with no active bookings
Endpoint : DELETE /Hospital/DeleteHospitalAsync/{hospitalId}
Input    : JWT (admin); no active bookings for this hospital
Expected : 204 No Content
           Hospital soft-deleted; all hospital-admin role mappings revoked

TC-HOSP-019: Admin cannot delete hospital with active bookings
Endpoint : DELETE /Hospital/DeleteHospitalAsync/{hospitalId}
Input    : JWT (admin); hospital has at least 1 active booking
Expected : 500 "has active bookings; cancel or transfer them before deleting"

TC-HOSP-020: Non-admin cannot delete hospital
Endpoint : DELETE /Hospital/DeleteHospitalAsync/{hospitalId}
Input    : JWT (user)
Expected : 403 Forbidden

================================================================================
MODULE 4: BOOKING
================================================================================

TC-BOOK-001: User creates a Dose1 booking successfully
Endpoint : POST /Booking/CreateBookingAsync
Input    : JWT (user A); { UserUid=userA.UserUid, Dose1HospitalUid=hosp1,
           Dose1RequestedDateTime=future, Dose1SlotNumber=1 }
Precond  : User has no existing active booking; hosp1.SlotsAvailable > 0
Expected : 201 Created
           BookingModel: all Dose2 fields empty/false
           hosp1.SlotsAvailable decremented by 1

TC-BOOK-002: User cannot create booking for another user
Endpoint : POST /Booking/CreateBookingAsync
Input    : JWT (user A); { UserUid = user B's UserUid }
Expected : 403 Forbidden

TC-BOOK-003: User cannot create booking with past date
Endpoint : POST /Booking/CreateBookingAsync
Input    : JWT (user); Dose1RequestedDateTime = yesterday
Expected : 500 "Dose1RequestedDateTime must be a future date"

TC-BOOK-004: User cannot create duplicate booking
Endpoint : POST /Booking/CreateBookingAsync
Input    : JWT (user); user already has an active booking
Expected : 500 "already has an active booking"

TC-BOOK-005: Booking fails when hospital has no available slots
Endpoint : POST /Booking/CreateBookingAsync
Input    : JWT (user); Dose1HospitalUid = hospital with SlotsAvailable = 0
Expected : 500 "has no available slots"

TC-BOOK-006: User books Dose2 after Dose1 is approved
Endpoint : PUT /Booking/BookDose2Async
Input    : JWT (user); { BookingId, Dose2HospitalUid, Dose2RequestedDateTime=future,
           Dose2SlotNumber }
Precond  : IsDose1Completed=true; Dose2HospitalUid is empty; hosp2 has slots
Expected : 200 OK; Dose2 fields populated; Dose2Hospital.SlotsAvailable -= 1

TC-BOOK-007: BookDose2 fails if Dose1 not yet completed
Endpoint : PUT /Booking/BookDose2Async
Input    : JWT (user); IsDose1Completed = false
Expected : 500 "Dose 1 must be completed before booking Dose 2"

TC-BOOK-008: BookDose2 fails if Dose2 already booked
Endpoint : PUT /Booking/BookDose2Async
Input    : JWT (user); Dose2HospitalUid not empty (already set)
Expected : 500 "Dose 2 is already booked"

TC-BOOK-009: BookDose2 fails if Dose1 was canceled
Endpoint : PUT /Booking/BookDose2Async
Input    : JWT (user); IsD1RequestCanceled = true
Expected : 500 "Dose 1 was canceled"

TC-BOOK-010: BookDose2 caller must own the booking
Endpoint : PUT /Booking/BookDose2Async
Input    : JWT (user A); BookingId belongs to user B
Expected : 403 Forbidden

TC-BOOK-011: Admin approves Dose1 booking
Endpoint : PUT /Booking/ApproveBookingsAsync/{bookingId}
Input    : JWT (admin); bookingId with IsDose1Completed=false, not canceled
Expected : 200 OK; IsDose1Completed=true; Dose1CompletedDateTime=UtcNow

TC-BOOK-012: hospital-admin approves booking at own hospital
Endpoint : PUT /Booking/ApproveBookingsAsync/{bookingId}
Input    : JWT (hospital-admin for hosp1); booking has Dose1HospitalUid=hosp1
Expected : 200 OK; IsDose1Completed=true

TC-BOOK-013: hospital-admin cannot approve booking at different hospital
Endpoint : PUT /Booking/ApproveBookingsAsync/{bookingId}
Input    : JWT (hospital-admin for hosp1); booking's Dose1HospitalUid=hosp2
Expected : 403 Forbidden

TC-BOOK-014: Normal user cannot approve booking
Endpoint : PUT /Booking/ApproveBookingsAsync/{bookingId}
Input    : JWT (user with no hospital-admin role)
Expected : 403 Forbidden

TC-BOOK-015: Approve Dose2 after Dose1 completed
Endpoint : PUT /Booking/ApproveBookingsAsync/{bookingId}
Input    : JWT (admin); IsDose1Completed=true, Dose2 booked, not yet approved
Expected : 200 OK; IsDose2Completed=true; IsVaccinationCompleted=true;
           VaccinationCompletedDateTime set

TC-BOOK-016: Approval fails if already fully vaccinated
Endpoint : PUT /Booking/ApproveBookingsAsync/{bookingId}
Input    : JWT (admin); IsVaccinationCompleted = true
Expected : 500 "vaccination is already fully completed"

TC-BOOK-017: Approval fails if no pending dose
Endpoint : PUT /Booking/ApproveBookingsAsync/{bookingId}
Input    : JWT (admin); IsD1RequestCanceled=true and Dose2 not booked
Expected : 500 "no pending dose to approve"

TC-BOOK-018: User cancels own Dose1 booking
Endpoint : PUT /Booking/CancelBookingsAsync/{bookingId}
Input    : JWT (user); IsDose1Completed=false, IsD1RequestCanceled=false
Expected : 200 OK; IsD1RequestCanceled=true
           Dose1Hospital.SlotsAvailable += 1

TC-BOOK-019: User cancels own Dose2 booking
Endpoint : PUT /Booking/CancelBookingsAsync/{bookingId}
Input    : JWT (user); IsDose1Completed=true, Dose2 booked, not completed
Expected : 200 OK; IsD2RequestCanceled=true
           Dose2Hospital.SlotsAvailable += 1

TC-BOOK-020: User cannot cancel another user's booking
Endpoint : PUT /Booking/CancelBookingsAsync/{bookingId}
Input    : JWT (user A); bookingId belongs to user B
Expected : 403 Forbidden

TC-BOOK-021: Cancel fails if vaccination already completed
Endpoint : PUT /Booking/CancelBookingsAsync/{bookingId}
Input    : JWT (user); IsVaccinationCompleted = true
Expected : 500 "vaccination is already completed and cannot be canceled"

TC-BOOK-022: Cancel fails when nothing to cancel
Endpoint : PUT /Booking/CancelBookingsAsync/{bookingId}
Input    : JWT (user); Dose1 canceled AND Dose2 not booked
Expected : 500 "no active dose request to cancel"

TC-BOOK-023: Admin updates a booking (full field override)
Endpoint : PUT /Booking/UpdateBookingAsync
Input    : JWT (admin); all booking fields with corrected values
Expected : 200 OK; booking updated as specified

TC-BOOK-024: Non-admin cannot use UpdateBookingAsync
Endpoint : PUT /Booking/UpdateBookingAsync
Input    : JWT (user)
Expected : 403 Forbidden ([Authorize(Roles="admin")] on endpoint)

TC-BOOK-025: User views own booking by booking ID
Endpoint : GET /Booking/GetBookingByBookingIdAsync/{bookingId}
Input    : JWT (user); bookingId belongs to caller
Expected : 200 OK; full booking profile

TC-BOOK-026: User cannot view another user's booking by ID
Endpoint : GET /Booking/GetBookingByBookingIdAsync/{bookingId}
Input    : JWT (user A); bookingId belongs to user B
Expected : 403 Forbidden

TC-BOOK-027: User views own booking by user UID
Endpoint : GET /Booking/GetBookingsByUserIdAsync/{userId}
Input    : JWT (user); userId = own UserUid
Expected : 200 OK

TC-BOOK-028: User cannot view another user's booking by user UID
Endpoint : GET /Booking/GetBookingsByUserIdAsync/{userId}
Input    : JWT (user A); userId = user B's UserUid
Expected : 403 Forbidden

TC-BOOK-029: Any user views bookings for a hospital
Endpoint : GET /Booking/GetBookingsByHospitalIdAsync/{hospitalId}
Input    : JWT (user); valid hospitalId
Expected : 200 OK; list of bookings for that hospital (Dose1 or Dose2)

TC-BOOK-030: Admin views all bookings
Endpoint : GET /Booking/GetAllBookingsAsync
Input    : JWT (admin)
Expected : 200 OK; all non-deleted bookings

TC-BOOK-031: Non-admin cannot view all bookings
Endpoint : GET /Booking/GetAllBookingsAsync
Input    : JWT (user)
Expected : 403 Forbidden

TC-BOOK-032: Check booking exists by booking ID
Endpoint : GET /Booking/IsBookingExistsAsync/{bookingId}
Input    : JWT (user); valid bookingId
Expected : 200 OK; true

TC-BOOK-033: Check booking does not exist
Endpoint : GET /Booking/IsBookingExistsAsync/{bookingId}
Input    : bookingId = "DOESNOTEXIST"
Expected : 200 OK; false

TC-BOOK-034: Admin deletes a booking (pending dose)
Endpoint : DELETE /Booking/DeleteBookingAsync/{bookingId}
Input    : JWT (admin); booking with pending Dose1
Expected : 204 No Content
           Booking soft-deleted; Dose1Hospital.SlotsAvailable += 1

TC-BOOK-035: Admin deletes a booking (both doses pending)
Endpoint : DELETE /Booking/DeleteBookingAsync/{bookingId}
Input    : JWT (admin); Dose1 pending + Dose2 booked but not approved
Expected : 204 No Content; both hospitals get +1 slot

TC-BOOK-036: Admin deletes a completed booking (no slot change)
Endpoint : DELETE /Booking/DeleteBookingAsync/{bookingId}
Input    : JWT (admin); IsVaccinationCompleted=true
Expected : 204 No Content; no SlotsAvailable change (doses already consumed)

TC-BOOK-037: Non-admin cannot delete booking
Endpoint : DELETE /Booking/DeleteBookingAsync/{bookingId}
Input    : JWT (user)
Expected : 403 Forbidden

================================================================================
MODULE 5: USER ROLE MAPPING
================================================================================

TC-ROLE-001: Admin assigns hospital-admin role to a user
Endpoint : POST /UserRoleMapping/AssignRoleAsync
Input    : JWT (admin); { UserUid: targetUserUid, RoleTag: "hospital-admin",
           ContextId: "HOSP001" }
Expected : 201 Created
           UserRoleMappings row: IsActive=true, RoleTag="hospital-admin",
           ContextId="HOSP001"

TC-ROLE-002: Non-admin cannot assign role
Endpoint : POST /UserRoleMapping/AssignRoleAsync
Input    : JWT (user)
Expected : 403 Forbidden (class-level [Authorize(Roles="admin")])

TC-ROLE-003: Assigning role to non-existent user
Endpoint : POST /UserRoleMapping/AssignRoleAsync
Input    : JWT (admin); { UserUid: "DOESNOTEXIST" }
Expected : 500 "user not found"

TC-ROLE-004: Assigning duplicate active role is blocked
Endpoint : POST /UserRoleMapping/AssignRoleAsync
Input    : JWT (admin); same UserUid + RoleTag + ContextId as existing active row
Expected : 500 "already has active role"

TC-ROLE-005: Reactivating a previously revoked role
Endpoint : POST /UserRoleMapping/AssignRoleAsync
Input    : JWT (admin); same UserUid + RoleTag + ContextId as IsActive=false row
Expected : 201 Created; existing row updated to IsActive=true (no duplicate created)

TC-ROLE-006: Admin revokes a role mapping
Endpoint : DELETE /UserRoleMapping/RevokeRoleAsync/{mappingId}
Input    : JWT (admin); mappingId = active role mapping
Expected : 204 No Content; IsActive=false on that row
           Immediate effect: user's next request fails hospital-admin check → 403

TC-ROLE-007: Revoking already-revoked mapping
Endpoint : DELETE /UserRoleMapping/RevokeRoleAsync/{mappingId}
Input    : JWT (admin); mappingId = already IsActive=false row
Expected : 500 "role mapping is already revoked" (idempotency guard)

TC-ROLE-008: Revoking non-existent mapping
Endpoint : DELETE /UserRoleMapping/RevokeRoleAsync/{mappingId}
Input    : JWT (admin); mappingId = 999999
Expected : 500 "role mapping not found"

TC-ROLE-009: Admin gets all roles for a user
Endpoint : GET /UserRoleMapping/GetUserRolesAsync/{userUid}
Input    : JWT (admin); userUid with 2 active role mappings + 1 revoked
Expected : 200 OK; list of 2 items (only IsActive=true returned)

TC-ROLE-010: Admin lists all users with a specific role
Endpoint : GET /UserRoleMapping/GetUsersInRoleAsync/{roleTag}
Input    : JWT (admin); roleTag = "hospital-admin" (no contextId)
Expected : 200 OK; all active hospital-admin mappings across all hospitals

TC-ROLE-011: Admin lists hospital-admins for specific hospital
Endpoint : GET /UserRoleMapping/GetUsersInRoleAsync/{roleTag}?contextId=HOSP001
Input    : JWT (admin); roleTag = "hospital-admin"; contextId = "HOSP001"
Expected : 200 OK; only mappings where ContextId = "HOSP001"

TC-ROLE-012: IsUserInRole returns true for active role
Endpoint : GET /UserRoleMapping/IsUserInRoleAsync/{userUid}/{roleTag}?contextId=HOSP001
Input    : JWT (admin); userUid with active hospital-admin for HOSP001
Expected : 200 OK; true

TC-ROLE-013: IsUserInRole returns false for revoked role
Endpoint : GET /UserRoleMapping/IsUserInRoleAsync/{userUid}/{roleTag}?contextId=HOSP001
Input    : JWT (admin); userUid with IsActive=false mapping for HOSP001
Expected : 200 OK; false

TC-ROLE-014: IsUserInRole returns false for wrong hospital
Endpoint : GET /UserRoleMapping/IsUserInRoleAsync/{userUid}/{roleTag}?contextId=HOSP002
Input    : JWT (admin); userUid has hospital-admin only for HOSP001
Expected : 200 OK; false

================================================================================
MODULE 6: CROSS-MODULE / CASCADE SCENARIOS
================================================================================

TC-CASCADE-001: Delete user cascades to bookings and slots
Steps:
  1. Register user A
  2. User A creates booking at Hospital X (SlotsAvailable = 50 → 49)
  3. User A books Dose2 at Hospital Y (SlotsAvailable = 50 → 49)
  4. Admin deletes user A (or user A deletes own account)
Expected:
  - User.IsDeleted = true
  - Booking soft-deleted
  - Hospital X: SlotsAvailable = 50 (Dose1 slot restored, was pending)
  - Hospital Y: SlotsAvailable = 50 (Dose2 slot restored, was pending)
  - UserCredentials.IsDeleted = true

TC-CASCADE-002: Delete user after Dose1 approved does not restore Dose1 slot
Steps:
  1. User creates booking at Hospital X (SlotsAvailable = 50 → 49)
  2. Admin approves Dose1 (IsDose1Completed = true)
  3. Admin deletes user
Expected:
  - Hospital X: SlotsAvailable remains 49 (Dose1 was administered, not pending)

TC-CASCADE-003: Delete hospital cascades to role mappings
Steps:
  1. Admin creates hospital H1
  2. Admin assigns user A as hospital-admin for H1
  3. Admin assigns user B as hospital-admin for H1
  4. Admin verifies no active bookings for H1
  5. Admin deletes H1
Expected:
  - H1.IsDeleted = true
  - Both user A and user B's role mappings: IsActive = false
  - User A and B can no longer call hospital-admin-gated endpoints for H1 → 403

TC-CASCADE-004: Cannot delete hospital with active booking
Steps:
  1. User creates booking with Dose1HospitalUid = H1 (Dose1 pending)
  2. Admin attempts to delete H1
Expected: 500 "has active bookings; cancel or transfer them before deleting"

TC-CASCADE-005: Full vaccination journey (happy path)
Steps:
  1. POST /User/CreateUserAsync           → user created
  2. POST /Auth/LoginAsync                → JWT obtained
  3. POST /Booking/CreateBookingAsync     → Dose1 booked, Hospital1.Slots -= 1
  4. PUT  /Booking/ApproveBookingsAsync   → Dose1 approved (by admin/hospital-admin)
  5. PUT  /Booking/BookDose2Async         → Dose2 booked, Hospital2.Slots -= 1
  6. PUT  /Booking/ApproveBookingsAsync   → Dose2 approved → IsVaccinationCompleted=true
Expected:
  - Booking.IsVaccinationCompleted = true
  - Booking.VaccinationCompletedDateTime set
  - Hospital1 slot permanently consumed (Dose1 completed)
  - Hospital2 slot permanently consumed (Dose2 completed)

TC-CASCADE-006: Cancel Dose1, then re-create booking at different hospital
Steps:
  1. User books Dose1 at Hospital1 (SlotsAvailable = 50 → 49)
  2. User cancels Dose1 (SlotsAvailable = 49 → 50)
  3. User attempts to create a new booking → blocked (booking still exists in DB)
Expected at step 3: 500 "already has an active booking"
  (Cancellation does not delete the booking; only admin DeleteBookingAsync removes it)

TC-CASCADE-007: Logout invalidates token across all requests
Steps:
  1. Login → receive JWT
  2. Successfully call /User/GetUserProfileDataAsync → 200
  3. POST /Auth/LogoutAsync → 200
  4. Retry /User/GetUserProfileDataAsync with same JWT
Expected at step 4: 401 Unauthorized

================================================================================
MODULE 7: SECURITY AND EDGE CASES
================================================================================

TC-SEC-001: Request without JWT returns 401
Endpoint : GET /User/GetAllUsersAsync
Input    : No Authorization header
Expected : 401 Unauthorized

TC-SEC-002: Request with tampered JWT returns 401
Endpoint : Any [Authorize] endpoint
Input    : Manually altered signature section of JWT
Expected : 401 (signature validation fails)

TC-SEC-003: Request with expired JWT returns 401
Endpoint : Any [Authorize] endpoint
Input    : JWT past ExpiresAt
Expected : 401 (ValidateLifetime=true, ClockSkew=Zero)

TC-SEC-004: Request with blacklisted JWT returns 401
Endpoint : Any [Authorize] endpoint
Input    : JWT whose jti is in RevokedTokens table
Expected : 401 (OnTokenValidated hook fires → context.Fail)

TC-SEC-005: User cannot escalate own role via UpdateUserAsync
Endpoint : PUT /User/UpdateUserAsync
Input    : JWT (user); request body includes UserRole: true
Expected : 200 OK BUT UserRole field is NOT updated
           (UpdateUserAsync does not touch UserRole — it is not in mutable fields)

TC-SEC-006: Non-admin JWT with role claim "admin" manually fabricated
Endpoint : POST /Hospital/CreateHospitalAsync
Input    : Token signed with wrong key but claim role="admin"
Expected : 401 (signature validation fails; fabricated token rejected)

TC-SEC-007: hospital-admin tries to approve booking for Dose1 at hosp1,
  but Dose1 is done and Dose2 is pending at hosp2 they do not manage
Endpoint : PUT /Booking/ApproveBookingsAsync/{bookingId}
Input    : JWT (hospital-admin for hosp1); Dose2 pending at hosp2
Expected : 403 Forbidden
  (approvingDose2 = true, relevantHospitalId = hosp2, IsUserInRoleAsync returns false)

TC-SEC-008: ForgotPassword does not leak whether email is registered
Endpoint : POST /Auth/ForgotPasswordAsync
Input 1  : { Email: "registered@email.com" }
Input 2  : { Email: "notregistered@email.com" }
Expected : Both return 200 OK with identical Message field.
  Only difference: ResetToken and ExpiresAt are null in Input 2 — but a client
  cannot distinguish "email not registered" from "email registered and we're
  withholding the token" since both return the same 200 status.

TC-SEC-009: PasswordResetToken cannot be replayed
Steps:
  1. ForgotPassword → get token T
  2. ResetForgottenPassword with T → success (T marked IsUsed=true)
  3. ResetForgottenPassword with T again
Expected at step 3: 500 (GetValidTokenAsync filters IsUsed=true)

================================================================================
END OF DOCUMENT
================================================================================
