info:
  name: get all users
  type: http

http:
  method: GET
  url: "{{baseURL}}/api/vaxtrack/v1/user/getallusers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
